# Normaliza fim de linha: o repo guarda LF, o checkout usa o do sistema.
# Sem isto, quem clonar no mac/Linux ve o arquivo inteiro como alterado.
* text=auto eol=lf

# O PEM do certificado nao pode ser tocado por conversao de EOL.
*.pem -text
